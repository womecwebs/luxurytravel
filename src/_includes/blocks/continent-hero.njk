<header class="continent-hero">

  <div class="hero-overlay">
    <div class="max-w-1100">

      <h1>{{ heroTitle }}</h1>

      <p>{{ heroDescription }}</p>

    </div>
  </div>

  {% if continent and continent.data and continent.data.heroImage %}
    <img
      src="{{ continent.data.heroImage }}"
      alt="{{ continent.data.title }}"
      class="hero-bg"
      loading="lazy"
      width="1200"
      height="800"
      fetchpriority="high"
    />
  {% endif %}

</header>
